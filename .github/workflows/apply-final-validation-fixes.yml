name: Apply final validation fixes

on:
  push:
    branches:
      - chatgpt/reference-ui-final

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout staging branch
        uses: actions/checkout@v4
        with:
          ref: chatgpt/reference-ui-final
          fetch-depth: 0

      - name: Apply verified patch
        shell: bash
        run: |
          git apply --check .chatgpt/final-validation-fixes.patch
          git apply .chatgpt/final-validation-fixes.patch
          rm -f .chatgpt/final-validation-fixes.patch
          rmdir .chatgpt 2>/dev/null || true
          rm -f .github/workflows/apply-final-validation-fixes.yml

      - name: Commit fixes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(test): align final validation with production UI"
          git push origin HEAD:chatgpt/reference-ui-final
